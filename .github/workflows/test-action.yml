name: Test Action

on:
  workflow_dispatch:   # allows manual trigger

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 23f1000704@ds.study.iitm.ac.in
        run: echo "Hello, this step contains the required email"

      - name: Done
        run: echo "Workflow completed"
